#include "Command.hpp"


void Command::invite(User user, std::string nickname, string channel)
{
	//check if channel exists
	//check if user exists and that it is part of the channel and user not in channel already
	//if in invite mode then check is user is an operator then add to invite list
	//if not just send the message
}