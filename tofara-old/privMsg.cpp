#include "Includes/Command.hpp"


//    Command: PRIVMSG
//   Parameters: <target>{,<target>} <text to be sent>
void command::privMessage(User user, std::string target, std::string message)
{
	//split based on comma
	//iterate through the target exists then send message to them them
	//check if
}